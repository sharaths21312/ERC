{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "particlegen.schema.json",
  "title": "Particle Generation",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "type": {
        "enum": ["particle", "flat", "particle_turret", "flat_turret"]
      },
      "target": {
        "description": "Target PARTIALLY IMPLEMENTED see funnelAndFieldtime in index.svelte.ts, only affects flat gen",
        "enum": ["all", "self", "notself", "onfield"]
      },
      "element": {
        "type": "string",
        "enum": ["Pyro", "Hydro", "Dendro", "Electro", "Cryo", "Anemo", "Geo", "None"]
      },
      "amount": {
        "description": "Per second for turrets, amounts per use otherwise",
        "type": "number"
      },
      "worst_case": {
        "description": "Worst case particle generation. Defaults to gen/floor(gen) for flat/particles and gen/2 for turrets THIS IS DIFFERENT TO THE SHEET",
        "type": "number"
      },
      "duration": {
        "description": "Duration for turrets",
        "type": "number"
      }
    },
    "required": ["type", "element", "amount"],
    "if": {
      "properties": {
        "type": { "const": "turret" }
      }
    },
    "then": {
      "properties": {
        "duration": true
      }
    }
  }
}